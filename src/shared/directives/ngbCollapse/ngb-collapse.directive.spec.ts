import { NgbCollapseDirective } from './ngb-collapse.directive';

describe('NgbCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = new NgbCollapseDirective();
    expect(directive).toBeTruthy();
  });
});
