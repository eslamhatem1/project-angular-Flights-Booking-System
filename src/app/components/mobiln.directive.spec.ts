import { MobilnDirective } from './mobiln.directive';

describe('MobilnDirective', () => {
  it('should create an instance', () => {
    const directive = new MobilnDirective();
    expect(directive).toBeTruthy();
  });
});
