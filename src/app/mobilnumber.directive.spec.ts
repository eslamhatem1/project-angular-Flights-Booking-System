import { MobilnumberDirective } from './mobilnumber.directive';

describe('MobilnumberDirective', () => {
  it('should create an instance', () => {
    const directive = new MobilnumberDirective();
    expect(directive).toBeTruthy();
  });
});
